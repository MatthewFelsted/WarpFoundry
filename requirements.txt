# codex-manager: all dependencies (main + brain + cua + dev)
# Install: pip install -r requirements.txt

# Core
pydantic>=2.0,<3
flask>=3.0
python-dotenv>=1.0
pyyaml>=6.0

# Brain (AI connectors)
openai>=1.0
anthropic>=0.30
google-generativeai>=0.7

# CUA (Computer-Using Agent)
playwright>=1.40

# Memory (Vector DB)
chromadb>=0.5

# Dev (testing, linting)
pytest>=8.0
ruff>=0.4
pytest-asyncio>=0.23
